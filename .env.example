# Supabase (optional — needed for login + public status page + leaderboard)
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# Landing page media assets (hero video, about image) are managed via the
# Admin panel → 🎬 站点素材 tab, stored in Supabase Storage + site_config table.
# No env var needed.
